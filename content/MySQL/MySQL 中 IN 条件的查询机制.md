+++
date = '2026-03-11T15:01:47+08:00'
draft = false
title = 'MySQL 中 IN 条件的查询机制'
+++

在 MySQL 中，`IN` 操作实际上是多个 `OR` 条件的简写形式。

当我们使用 `IN` 执行查询时，MySQL 优化器会根据场景选择不同策略。比如：

- 当 `IN` 列表是常量且已排序，可能使用二分查找
- 当 `IN` 列表是子查询时，可能逐行比较
