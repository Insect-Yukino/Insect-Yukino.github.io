+++
date = '2026-03-11T13:59:18+08:00'
draft = false
title = '树'
+++

树

**深度**：从根节点到该节点边数，方向是自顶向下。

**高度**：从该节点到最远叶子节点的边数，方向是自底向上

基准点都是该节点，该节点高出叶子节点叫高度，根节点到叶子节点叫深度

求数的高度

```java
int height(TreeNode root) {
    if (root == null) return -1;
    int left = height(root.left);
    int right = height(root.right);
    return Math.max(left, right) + 1;
}
```

该方法采用递归的方式，当 `root == null` 出栈，自底向上计算。
